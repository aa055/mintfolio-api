# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data layer
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async Postgres driver (runtime)
psycopg[binary]==3.2.3   # sync Postgres driver (Alembic migrations)

# Validation + settings
pydantic==2.10.3
pydantic-settings==2.6.1

# Auth (verifying Supabase JWTs)
python-jose[cryptography]==3.3.0

# HTTP client (GoldAPI, Supabase REST calls)
httpx==0.27.2

# Multipart parsing (file uploads)
python-multipart==0.0.17

# Date/time
python-dateutil==2.9.0.post0
